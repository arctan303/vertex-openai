# Vertex AI API Key（必需）
VERTEX_API_KEY=your-api-key

# 代理服务器监听端口（默认 3000）
PORT=3000

# 自定义 API Key（可选，全局 key，留空则不验证）
API_KEY=

# 管理后台密钥（可选，保护 /admin 页面）
ADMIN_KEY=

# 默认模型（当请求未指定模型时使用）
DEFAULT_MODEL=gemini-2.0-flash
